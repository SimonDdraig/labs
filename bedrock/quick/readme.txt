Four jupyter notebooks

Text Generation
Uses a number of FMs to demonstrate text generation, summary, etc from text prompt

Image Generation
Uses a number of FMs to demonstrate generation of images from text prompt

Image Parsing
Uses a number of FMs to show how an image can be parsed and data extracted from it from text prompt
Uses receipt example in the resources folder

Video Generation
Uses a number of FMs to generate video from text prompt